# Code generated from templates/callers/github-ci.yml.tmpl; replace the zero SHA with an approved release commit.
name: github-ci

on:
  pull_request:
  push:
    branches: [main]
  merge_group:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  gate:
    permissions:
      contents: read
      security-events: write  # CodeQL publishes results evaluated by the local gate.
    uses: gomaja/github-ci/.github/workflows/go.yml@0000000000000000000000000000000000000000
